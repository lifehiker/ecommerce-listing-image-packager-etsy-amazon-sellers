export function WelcomeEmail({ name }: { name: string }) {
  return (
    <main>
      <h1>Welcome to ListingPackr, {name}</h1>
      <p>Create an Etsy or Amazon image pack, rename by SKU, watermark previews, and export a ZIP.</p>
    </main>
  );
}

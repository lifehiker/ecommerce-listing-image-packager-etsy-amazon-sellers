# BUILD INSTRUCTIONS: ecommerce-listing-image-packager-etsy-amazon-sellers

## Overview

- **App Name:** ecommerce-listing-image-packager-etsy-amazon-sellers
- **Repository:** ecommerce-listing-image-packager-etsy-amazon-sellers
- **Tech Stack:** Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- **Description:** A web app that turns batches of product images, screenshots, and mockups into marketplace-ready listing image sets. It can resize, compress, watermark, rename by SKU/ASIN, order images to platform requirements, and package assets for Etsy or Amazon listings.

---

## Tech Stack & Setup

### Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui

```bash
npx create-next-app@latest ecommerce-listing-image-packager-etsy-amazon-sellers --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
cd ecommerce-listing-image-packager-etsy-amazon-sellers
# shadcn/ui (always include)
npx shadcn@latest init -d
npx shadcn@latest add button card input label form toast badge
# Only add Prisma if the PRD requires data persistence:
# npm install prisma @prisma/client
# npx prisma init
# Only add NextAuth if the PRD requires user login:
# npm install next-auth@beta @auth/prisma-adapter bcryptjs
# npm install -D @types/bcryptjs
# Only add Stripe if the PRD explicitly requires payments:
# npm install stripe @stripe/stripe-js
# Only add Resend if the PRD explicitly requires email:
# npm install resend
```

### Configuration

Add to next.config.ts:
```ts
const nextConfig = {
  output: "standalone",
};
export default nextConfig;
```

### Development

```bash
npm run dev
```

### Build

```bash
npm run build
```

---

## Product Requirements Document

The full PRD is included below. Implement ALL features described.

---

# Ecommerce Listing Image Packager for Etsy and Amazon Sellers

*Generated by Forge on 2026-05-25T11:12:16.987Z — score 8.4/10*

## Overview

**ListingPackr is a web SaaS** for Etsy digital pattern sellers and Amazon/private-label sellers who repeatedly prepare batches of product images for marketplace listings. Instead of using Canva, TinyPNG, manual watermarking, spreadsheet SKU naming, and ZIP folders separately, the product lets sellers upload product images, screenshots, mockups, and pattern previews, then export marketplace-ready image packs with correct sizing, compression, watermarking, ordered image slots, SKU/ASIN-based filenames, and ZIP packaging for Etsy or Amazon listing workflows.

## Why this will succeed

### Comparable products that are working

1. **Canva** — https://www.canva.com  
   - Research notes Canva has publicly reported **over 170M monthly active users** and Canva Pro is commonly priced around **$15/month or $120/year**.  
   - What works: Canva validates that creators and sellers pay for visual asset creation tools, templates, resizing, and repeatable design workflows.

2. **Placeit** — https://placeit.net  
   - Research identifies Placeit as a large mockup/template marketplace owned by Envato, with subscription pricing commonly in the low-to-mid monthly SaaS range.  
   - What works: Placeit validates demand for product mockups and template-based commerce imagery, especially for sellers who need repeatable listing visuals.

3. **Vela** — https://getvela.com  
   - Research identifies Vela as a known Etsy and Shopify bulk listing editor. Public exact user count is not clearly disclosed.  
   - What works: Vela validates that marketplace sellers pay for workflow tools that reduce repetitive listing-management work.

### Specific data points from research

- Etsy has reported **millions of active sellers**, which supports a large overall marketplace seller ecosystem.
- The research estimates a focused niche of **3,000–8,000 semi-serious Etsy sellers** offering cross-stitch, embroidery, or needlework digital patterns.
- The revenue model assumes a focused base of **150–250 active sellers** can support approximately **$3,000–$6,000/month** at roughly $20–$25 blended monthly revenue.
- The optimistic path assumes **700–1,000 sellers** across Etsy digital patterns, handmade goods, and Amazon private-label workflows at a $25–$30 blended monthly price, supporting **$15,000–$30,000 MRR**.
- The opportunity was identified from both **Product Hunt and Hacker News research sources**, which is a cross-source signal that the pain is visible outside one isolated niche. The research does not provide specific thread titles, URLs, comment counts, or voting data, so this should be treated as directional evidence, not hard proof.
- Target keywords from the research map directly to buildable product jobs:
  - `etsy cross stitch listing photos`
  - `sell cross stitch patterns on etsy`
  - `cross stitch pattern mockup generator`
  - `etsy digital pattern image size`
  - `watermark cross stitch pattern`
  - `amazon listing image organizer tool`
  - `bulk product photo renamer for sellers`
  - `private label product image management`
  - `product image organizer`
  - `listing image packager`
  - `etsy images`
  - `amazon image requirements`
  - `sku renaming`
  - `image resizing`
  - `watermarking`

### Why this specific opportunity is different / better

This should not compete as a generic image editor. Canva and Placeit already cover generic design and mockups better than a bootstrapped solo developer can. The wedge is **marketplace-ready image packaging**, especially for sellers publishing similar listings repeatedly.

The exploitable gaps from the research are:

- **Canva gap:** Canva is powerful but generic. It does not package Etsy/Amazon image sets around SKU naming, listing image order, export ZIPs, platform profiles, or bulk operational workflows.
- **Placeit gap:** Placeit is strong for mockups but not focused on Etsy digital pattern workflows such as stitch-grid previews, palette cards, anti-theft watermarking, or final listing-pack export.
- **Vela gap:** Vela focuses on bulk listing editing, not generating, resizing, watermarking, compressing, renaming, and packaging image assets before upload.

The product wins if it is positioned as:  
**“Create a complete Etsy or Amazon listing image pack in one upload.”**  
Not:  
**“Another image resizer.”**

## How this will succeed

### Primary channel

**Primary channel: SEO**

SEO is the best first channel because the research identifies specific high-intent long-tail keywords tied directly to pain points and features: Etsy image size, digital pattern image size, watermarking cross-stitch patterns, cross-stitch mockups, Amazon image requirements, bulk product photo renaming, and SKU image organization.

These are not broad awareness keywords. They imply the seller is actively preparing a listing and needs a practical workflow. The product can capture this intent with free single-use tools that lead into the paid batch workflow.

**Secondary channel:** community seeding in Etsy seller and cross-stitch communities, using workflow tutorials and before/after examples rather than a generic SaaS announcement.

### Channel-specific playbook

#### SEO keyword cluster

The research does not provide verified monthly search volumes. Do not invent them. Before final launch, validate exact volumes and difficulty using Google Search Console after indexing, Google Keyword Planner, Ahrefs, Semrush, or LowFruits. For MVP planning, prioritize by buyer intent rather than assumed volume.

Primary keyword clusters:

1. **Etsy image requirements**
   - `etsy digital pattern image size`
   - `etsy images`
   - `etsy listing image size`
   - `etsy digital download image size`

2. **Cross-stitch seller workflow**
   - `etsy cross stitch listing photos`
   - `sell cross stitch patterns on etsy`
   - `cross stitch pattern mockup generator`
   - `watermark cross stitch pattern`

3. **Amazon image packaging**
   - `amazon image requirements`
   - `amazon listing image organizer tool`
   - `amazon product image file naming`
   - `asin image naming`

4. **Bulk seller utilities**
   - `bulk product photo renamer for sellers`
   - `sku renaming`
   - `product image organizer`
   - `listing image packager`
   - `image resizing`
   - `watermarking`

#### Current SERP competition

The research did not include live SERP snapshots, ranking URLs, or keyword difficulty. The founder should manually inspect the first page before writing each page. Expected competitor categories likely include:

- Official marketplace documentation, such as Etsy Help and Amazon Seller Central documentation.
- Generic design/image tools, such as Canva.
- Blog posts from Etsy seller educators.
- Generic image compressor/resizer utilities.

A new site can realistically break in first on long-tail workflow pages where the searcher wants a tool, not just documentation. The best first targets are not broad phrases like `image resizing`; they are specific phrases like `watermark cross stitch pattern` and `bulk product photo renamer for sellers`.

#### First 10 SEO pages to build

Each page should include:
- A concise answer to the search intent.
- A free single-use tool.
- A CTA to “Create a full listing image pack.”
- Examples for Etsy/cross-stitch or Amazon workflows.
- Internal links to the core app and related tools.

1. `/tools/etsy-digital-pattern-image-size`
   - Free tool: resize one image to Etsy-friendly dimensions.
   - CTA: batch resize and package all listing images.

2. `/tools/watermark-cross-stitch-pattern`
   - Free tool: upload one pattern preview and apply diagonal watermark.
   - CTA: save reusable watermark templates.

3. `/tools/cross-stitch-pattern-mockup-generator`
   - Free tool: generate one mockup-style preview from an uploaded pattern image.
   - CTA: create full Etsy pattern image set.

4. `/tools/etsy-cross-stitch-listing-photos`
   - Guide + checklist for listing photo order.
   - Free downloadable listing image checklist.
   - CTA: auto-order images into listing slots.

5. `/tools/bulk-product-photo-renamer`
   - Free tool: rename up to 5 images using SKU prefix.
   - CTA: bulk SKU rules, ZIP export, saved presets.

6. `/tools/sku-image-renamer`
   - Free tool: rename files using `{sku}-{slot}` pattern.
   - CTA: saved naming templates.

7. `/tools/amazon-listing-image-organizer`
   - Free checklist and drag/drop image ordering tool.
   - CTA: Amazon export profile and ASIN naming.

8. `/guides/amazon-image-requirements`
   - Explain image-prep workflow.
   - CTA: resize/compress/order package.

9. `/tools/listing-image-packager`
   - Main commercial landing page for the SaaS.
   - CTA: start free trial or create first pack.

10. `/guides/sell-cross-stitch-patterns-on-etsy`
   - Workflow article covering mockups, watermarking, image order, and theft deterrence.
   - CTA: create a cross-stitch listing image pack.

### Week-by-week execution plan

#### Week 4: beta + warm-up

By Week 4, the MVP should be functional enough to create and export a complete listing image pack.

Marketing prep:

- Publish the first 5 SEO pages:
  1. `/tools/etsy-digital-pattern-image-size`
  2. `/tools/watermark-cross-stitch-pattern`
  3. `/tools/bulk-product-photo-renamer`
  4. `/tools/listing-image-packager`
  5. `/guides/sell-cross-stitch-patterns-on-etsy`
- Set up:
  - Google Search Console
  - Google Analytics or Plausible
  - PostHog for funnel events
  - Stripe test mode
  - Resend transactional emails
- Build outreach list of 50–100 Etsy/cross-stitch sellers from public shops, blogs, YouTube channels, and communities. Do not scrape private data.
- Warm community accounts for at least 2 weeks before launch ask:
  - r/EtsySellers
  - r/Etsy
  - r/CrossStitch
  - Etsy Community forums
  - Facebook groups for Etsy digital product sellers and cross-stitch pattern designers where self-promotion rules allow participation
- Value-first warm-up posts:
  - “Checklist I use for Etsy digital pattern listing images”
  - “How do you watermark pattern previews without making them ugly?”
  - “What’s your current image workflow for new Etsy listings?”
  - “I made a simple SKU file naming template for product photos — feedback welcome”

#### Week 5: launch

Launch goal: drive initial beta users, collect workflow feedback, and get first paid conversions from sellers who publish new listings monthly.

Day-by-day plan:

**Monday**
- Publish `/tools/cross-stitch-pattern-mockup-generator`.
- Post to r/EtsySellers around late morning US Eastern:
  - Title: “I made a free Etsy listing image checklist + batch image packager for digital pattern sellers — looking for workflow feedback”
  - Value proposition: free checklist and free single-pack export, no hard sell.
- Email 20 sellers from the outreach list with a personalized note:
  - “I noticed you publish digital pattern listings. I built a tool that creates watermarked, correctly named Etsy image packs. Could I package one listing for you and get feedback?”

**Tuesday**
- Publish `/tools/etsy-cross-stitch-listing-photos`.
- Post a short workflow demo in allowed Etsy seller communities:
  - Before: folder of random screenshots/mockups.
  - After: ordered ZIP with `SKU-01-main.jpg`, `SKU-02-preview.jpg`, etc.
- DM/email another 20 sellers.

**Wednesday**
- Publish `/tools/sku-image-renamer`.
- Post to r/CrossStitch only if rules allow seller workflow discussion:
  - Title: “Question for pattern designers: how do you show previews without giving away too much of the chart?”
  - Do not lead with the product. Lead with watermarking examples and ask for feedback.

**Thursday**
- Publish `/tools/amazon-listing-image-organizer`.
- Post in Amazon/private-label seller spaces only where allowed:
  - Title: “Free SKU image renamer + listing image order checklist”
- Email 20 Amazon/private-label sellers or small agencies if identified.

**Friday**
- Publish a launch article:
  - `/blog/how-to-create-a-marketplace-ready-listing-image-pack`
- Send beta recap email:
  - “Here are the 5 most common listing image prep mistakes I saw this week”
- Offer 30-day beta discount only to feedback participants, not a permanent cheap plan.

**Saturday/Sunday**
- Review analytics:
  - Landing page visits
  - Tool usage
  - Upload start rate
  - Export completion rate
  - Signup rate
  - Trial-to-paid intent
- Interview 3–5 users via email or Zoom.

### Weeks 6–8: compound

Repeat weekly:

- Publish 2 SEO pages or comparison/workflow articles per week.
- Add 1 free utility page every 1–2 weeks.
- Email 20–30 targeted sellers per week with personalized workflow examples.
- Post 1–2 value-first community posts per week, only in communities where the founder has been participating.
- Convert common support questions into SEO pages.

One-time improvements:

- Week 6: add saved presets and improve export speed.
- Week 7: add cross-stitch-specific watermark templates and sample listing packs.
- Week 8: add Amazon profile improvements if Amazon users show meaningful activation.

If SEO underperforms by Week 6:

- Do not abandon SEO, because ranking takes time.
- Shift short-term acquisition toward community seeding and direct seller outreach.
- Use outreach conversations to identify the highest-converting niche:
  - Etsy cross-stitch digital pattern sellers
  - Etsy digital downloads more broadly
  - Amazon/private-label sellers
- Narrow homepage positioning to the group with highest activation.

### 3-month targets

Because search volume numbers were not provided in the research, targets should focus on controllable SEO production, indexing, and funnel metrics rather than fabricated ranking guarantees.

By end of Month 3:

- 25–35 indexed SEO/tool pages.
- 10 pages receiving impressions in Google Search Console.
- 3 pages ranking on page 1–2 for at least one long-tail query.
- 1,500–3,000 total site visitors from SEO, communities, and outreach.
- 300–500 free tool uses.
- 100–150 registered users.
- 30–50 users creating at least one full image pack.
- 15–30 paying subscribers.

### Budget & CAC

3-month cash budget: **$1,500–$4,000**, matching the research estimate.

Suggested allocation:

- $300–$600: keyword tooling, LowFruits/Ahrefs/Semrush short-term subscription, or equivalent.
- $300–$700: design examples, sample mockups, or outsourced illustration assets if needed.
- $500–$1,500: small creator sponsorships in Etsy seller or cross-stitch communities.
- $200–$500: analytics, email, storage, and infrastructure buffer.
- $200–$700: optional content editing or screenshots/tutorial production.

Founder time budget:

- Weeks 1–4: 25–35 hours/week build + 5–8 hours/week marketing prep.
- Weeks 5–8: 15–25 hours/week build/support + 10–15 hours/week SEO/community/outreach.
- Month 3: 10–15 hours/week product + 10–15 hours/week distribution.

Expected CAC:

- Research estimates **$10–$45 CAC**.
- SEO and community-led acquisition should be near-zero cash CAC but high founder-time CAC.
- Paid creator sponsorships should be treated as tests, not the core growth engine.

### Why this works at zero/low cost

The playbook works with no audience because the product attaches to existing high-intent tasks: sellers already search for image sizes, watermarking, mockups, SKU renaming, and Amazon/Etsy image requirements. Free single-use tools give the founder something useful to share in communities and outreach without asking strangers to buy a generic SaaS.

## Target user

Primary persona: **semi-serious Etsy digital pattern seller**, especially cross-stitch, embroidery, or needlework pattern designers.

They:

- Publish new designs monthly or in seasonal batches.
- Sell digital downloads on Etsy.
- Need listing images that show the finished look, preview the pattern, communicate what is included, and deter theft.
- Currently use a mix of Canva, screenshots, TinyPNG, manual file renaming, and ad hoc watermarking.
- Often reuse a similar image format across many listings.
- Care about speed because listing prep is repetitive.
- Care about anti-theft watermarking because digital pattern previews can reveal too much.
- Hang out in:
  - r/EtsySellers
  - r/Etsy
  - r/CrossStitch
  - Etsy Community forums
  - Facebook groups for Etsy sellers, digital product sellers, and cross-stitch pattern designers
  - YouTube/blog ecosystems around selling digital products on Etsy

Secondary persona: **small Amazon/private-label seller or VA/agency assistant** who prepares product image folders and needs consistent SKU/ASIN naming, ordering, compression, and ZIP export.

The MVP should prioritize the Etsy digital pattern seller because the workflow is more differentiated than generic Amazon image organization.

## Core features (MVP)

- **Upload image batch**
  - Drag-and-drop upload.
  - Accept JPG, PNG, and WebP.
  - Show thumbnails, filenames, dimensions, and file sizes.
  - Limit by plan.

- **Marketplace profile selector**
  - Profiles:
    - Etsy digital listing
    - Etsy physical product
    - Amazon product listing
  - Each profile defines default output format, max dimensions, filename pattern, and recommended image slots.

- **Image slot ordering**
  - Drag images into ordered slots:
    - Main image
    - Pattern preview
    - Finished mockup
    - Detail close-up
    - Size/instructions
    - Palette/materials
    - What’s included
  - For Amazon:
    - Main
    - Lifestyle
    - Feature
    - Detail
    - Infographic
    - Size/dimensions
  - Allow custom slot labels.

- **Batch resize**
  - Resize images to selected width/height mode:
    - Fit within max dimensions
    - Square crop
    - Keep original aspect ratio
  - Default Etsy profile should use a configurable “listing image” preset rather than hardcoding unverified requirements.

- **Batch compression**
  - Compress JPG/WebP outputs.
  - Quality slider from 60–95.
  - Show estimated output size.

- **Watermarking**
  - Add text watermark.
  - Options:
    - Diagonal repeated watermark
    - Center watermark
    - Bottom-right watermark
  - Controls:
    - Text
    - Opacity
    - Font size
    - Rotation
    - Color
  - Save watermark template for paid users.

- **SKU/ASIN filename renaming**
  - Input SKU, product code, or ASIN.
  - Filename pattern:
    - `{sku}-{slotNumber}-{slotName}.jpg`
    - `{asin}-{slotNumber}.jpg`
    - `{original}-{slotNumber}.jpg`
  - Preview final filenames before export.

- **ZIP export**
  - Generate a ZIP containing processed images.
  - Include optional `manifest.csv` with:
    - Original filename
    - Output filename
    - Slot name
    - Dimensions
    - File size
    - Watermark applied true/false

- **Saved presets**
  - Paid users can save:
    - Marketplace profile
    - Filename pattern
    - Watermark settings
    - Compression settings
    - Slot order

- **Free single-use tools**
  - Single-image resizer.
  - Single-image watermarker.
  - 5-file SKU renamer.
  - These power SEO landing pages and lead to SaaS signup.

- **Account dashboard**
  - Recent packs.
  - Saved presets.
  - Usage limits.
  - Billing link.

- **Stripe subscription gating**
  - Free trial or limited free use.
  - Paid plan unlocks larger batches, saved presets, and repeated exports.

## Tech stack recommendation

Use the standard Forge-compatible web SaaS stack:

- **Next.js 15 App Router + TypeScript**
- **PostgreSQL + Prisma ORM**
- **Tailwind CSS + shadcn/ui**
- **NextAuth v5 with Google OAuth**
- **Stripe subscriptions**
- **Resend transactional email**
- **Image processing**
  - Use browser-side Canvas for simple free tools where possible.
  - Use server-side `sharp` for authenticated batch processing and consistent exports.
  - Use `jszip` for ZIP generation if done client-side, or `archiver` server-side.
- **File storage**
  - MVP should avoid permanent original-image storage if possible.
  - Process files temporarily and delete after export.
  - Store only metadata, presets, and pack records.
  - If persistent storage becomes necessary, add S3-compatible object storage later.
- **Deployment**
  - Coolify with Docker-based deployment.
  - Next.js standalone output.

No mobile framework is needed. This should be a responsive web app that works well on desktop and tablet. The primary workflow involves batch file upload and image management, so desktop web is the default experience.

## Monetization & pricing

Primary strategy: **subscription SaaS with limited free tools for acquisition.**

Pricing should reflect workflow value, not compete with generic cheap image tools. Comparable products in the research sit in paid creator/seller workflow ranges:

- Canva Pro around **$15/month or $120/year**.
- Placeit/Envato-style mockup subscriptions in low-to-mid monthly SaaS ranges.
- Etsy seller tools such as Vela, eRank, and Marmalead validate recurring seller-tool subscriptions.

Recommended tiers:

### Free Tools — $0

Purpose: SEO acquisition and product sampling.

Includes:

- Single-image resize.
- Single-image watermark.
- Rename up to 5 files at once.
- Export one small image pack with watermark “Created with ListingPackr” on manifest only, not on seller images.
- No saved presets.
- No saved history.

### Solo — $19/month or $190/year

For Etsy digital sellers publishing regularly.

Includes:

- Up to 100 processed images/month.
- Up to 25 images per pack.
- Etsy marketplace profiles.
- Batch resizing and compression.
- SKU filename patterns.
- ZIP export.
- 3 saved presets.
- Watermark templates.
- Recent pack history.

### Pro — $39/month or $390/year

For high-volume Etsy sellers, Amazon sellers, and assistants.

Includes:

- Up to 500 processed images/month.
- Up to 100 images per pack.
- Etsy and Amazon profiles.
- Unlimited saved presets.
- Advanced filename rules.
- Manifest CSV export.
- Priority processing.
- Multiple watermark templates.

### Optional annual hobby offer — $99/year

Only offer this as an annual-only lightweight plan for sellers who publish seasonally.

Includes:

- 300 processed images/year.
- Etsy profiles only.
- 5 saved presets.
- No Amazon profiles.

Do not offer a $5/month plan. It would attract low-frequency users who are less likely to value repeated workflow automation.

## Keyword / SEO strategy

The SEO strategy should use a tool-led content structure. Each high-intent page should provide immediate utility, then upsell the full batch workflow.

### Primary SEO keywords

The research provides target keywords but no verified search volume. Treat volume as unverified until measured.

Primary targets:

- `etsy digital pattern image size`
- `watermark cross stitch pattern`
- `cross stitch pattern mockup generator`
- `etsy cross stitch listing photos`
- `bulk product photo renamer for sellers`
- `sku renaming`
- `amazon listing image organizer tool`
- `amazon image requirements`
- `product image organizer`
- `listing image packager`

### Long-tail targets

- `how to watermark cross stitch pattern for etsy`
- `best image size for etsy digital downloads`
- `how to sell cross stitch patterns on etsy`
- `etsy digital download listing photo examples`
- `rename product photos by sku`
- `bulk rename product images for amazon`
- `organize amazon listing images`
- `etsy pattern preview watermark`
- `cross stitch pattern listing image template`
- `compress etsy listing photos`

### Meta title format

Use practical, utility-first titles:

- `{Task} Tool for {Seller Type} | ListingPackr`
- `{Marketplace} Image Size Guide + Free Resizer | ListingPackr`
- `Watermark {Product Type} for Etsy | Free Tool`

Examples:

- `Etsy Digital Pattern Image Size Guide + Free Resizer | ListingPackr`
- `Watermark Cross Stitch Patterns for Etsy | Free Tool`
- `Bulk Product Photo Renamer for SKU Images | ListingPackr`

### Meta description format

Use:

`Prepare {marketplace/use case} images faster. Resize, watermark, rename, order, and export listing-ready image packs with a free {tool type}.`

Example:

`Prepare Etsy digital pattern images faster. Resize, watermark, rename by SKU, and export listing-ready image packs with a free watermarking tool.`

### Site structure

- `/` — main SaaS homepage
- `/app` — authenticated dashboard
- `/app/packs/new` — create new image pack
- `/app/packs/[id]` — pack editor
- `/app/presets` — saved presets
- `/app/billing` — billing
- `/tools` — free tools hub
- `/tools/etsy-digital-pattern-image-size`
- `/tools/watermark-cross-stitch-pattern`
- `/tools/cross-stitch-pattern-mockup-generator`
- `/tools/bulk-product-photo-renamer`
- `/tools/sku-image-renamer`
- `/tools/amazon-listing-image-organizer`
- `/guides` — SEO guides hub
- `/guides/sell-cross-stitch-patterns-on-etsy`
- `/guides/amazon-image-requirements`
- `/blog` — launch updates and workflows

### Pages to prioritize

Priority 1:

1. Etsy digital pattern image size
2. Watermark cross-stitch pattern
3. Bulk product photo renamer
4. Listing image packager
5. Sell cross-stitch patterns on Etsy

Priority 2:

6. Cross-stitch pattern mockup generator
7. Etsy cross-stitch listing photos
8. SKU image renamer
9. Amazon listing image organizer
10. Amazon image requirements

## Revenue model

Revenue projections should be tied to the SEO/community execution plan and the research’s conservative/optimistic ranges.

### Funnel assumptions

Month 1 is mostly beta and outreach-driven. Month 3 begins to show SEO impressions and early rankings. Month 6 and Month 12 depend on consistent content publishing and product retention among sellers who publish monthly.

Assumed funnel:

- Visitor to free tool use: 10–20%
- Free tool user to signup: 15–30%
- Signup to activated pack export: 30–50%
- Activated user to paid: 15–30%

These are planning assumptions and should be replaced with actual PostHog/Stripe data after launch.

### Month 1

Expected:

- 300–700 visitors from community posts, outreach, and initial SEO indexing.
- 75–150 free tool uses.
- 30–60 signups.
- 10–20 activated users.
- 3–8 paid subscribers.

Revenue:

- Mostly Solo at $19/month.
- 3–8 paid users.
- Projected MRR: **$57–$152**.

### Month 3

Expected:

- 1,500–3,000 cumulative visitors.
- 300–500 free tool uses.
- 100–150 signups.
- 30–50 activated users.
- 15–30 paid subscribers.

Revenue:

- Blended ARPU: $22–$27/month.
- Projected MRR: **$330–$810**.

This is below the research’s conservative mature MRR range because SEO takes time and the first 3 months are validation-heavy.

### Month 6

Expected:

- 8,000–15,000 cumulative visitors if 40–60 SEO/tool pages are published and indexed.
- 1,500–3,000 free tool uses.
- 500–900 signups.
- 150–300 activated users.
- 75–150 paid subscribers.

Revenue:

- Blended ARPU: $23–$28/month.
- Projected MRR: **$1,725–$4,200**.

This approaches the lower end of the research’s conservative range.

### Month 12

Expected:

- 30,000–60,000 cumulative visitors from SEO, community referrals, and direct searches.
- 5,000–10,000 free tool uses.
- 1,500–3,000 signups.
- 500–1,000 activated users.
- 150–300 paid subscribers conservative case.
- 500–800 paid subscribers strong case.

Revenue:

- Conservative blended ARPU: $23–$28/month.
  - 150–300 paid users = **$3,450–$8,400 MRR**
- Strong case blended ARPU: $25–$30/month.
  - 500–800 paid users = **$12,500–$24,000 MRR**

This aligns with the research’s conservative projection of **$3,000–$6,000/month** and optimistic path toward **$15,000–$30,000/month**, assuming SEO execution compounds and sellers retain because they publish listings repeatedly.

## Competitor landscape

### Canva

- URL: https://www.canva.com
- Pricing model: Canva Pro commonly around $15/month or $120/year, per research.
- Key features:
  - Design editor.
  - Templates.
  - Image resizing.
  - Brand kits.
  - Mockups and visual content tools.
- Specific gaps:
  - Generic design tool, not a marketplace image packaging workflow.
  - Does not natively organize Etsy/Amazon listing image sets by slot order.
  - Does not solve SKU/ASIN filename rules and ZIP export as a core workflow.
  - Batch workflows require manual setup.
- Differentiation:
  - ListingPackr is not a design canvas. It is a batch packaging pipeline for sellers who already have product images, screenshots, and mockups and need them marketplace-ready fast.

### Placeit

- URL: https://placeit.net
- Pricing model: Subscription pricing in the low-to-mid monthly SaaS range, per research.
- Key features:
  - Mockup templates.
  - Product visuals.
  - Apparel and commerce imagery.
  - Template marketplace.
- Specific gaps:
  - Not focused on Etsy digital pattern workflows.
  - Does not handle stitch-grid previews, palette cards, anti-theft watermarking, or Etsy digital pattern image packs.
  - Does not solve operational export packaging by SKU, slot order, and marketplace profile.
- Differentiation:
  - ListingPackr can include lightweight mockup generation later, but the MVP wins on final preparation: watermark, resize, rename, order, and ZIP.

### Vela

- URL: https://getvela.com
- Pricing model: Etsy/Shopify bulk listing editor; exact public user count and pricing details were not provided in the research.
- Key features:
  - Bulk listing editing.
  - Etsy and Shopify workflow support.
  - Marketplace listing management.
- Specific gaps:
  - Focused on editing listings, not preparing image assets before upload.
  - Does not directly solve batch resizing, watermarking, compression, filename rules, and packaged image export.
- Differentiation:
  - ListingPackr sits before the listing editor. It creates clean, marketplace-ready asset folders that can be uploaded into Etsy, Amazon, or another listing workflow.

## Timeline

### Week 1: Core image pack workflow

Build:

- Next.js project setup.
- Authentication.
- Database schema.
- Dashboard shell.
- New pack creation.
- Upload UI with drag-and-drop.
- Image thumbnail grid.
- Marketplace profile selector.
- Slot ordering UI.
- Basic filename preview.

Deliverable:

- Authenticated user can create a pack, upload images, assign slots, enter SKU, and preview output filenames.

### Week 2: Processing and export

Build:

- Server-side image processing with `sharp`.
- Resize settings.
- Compression settings.
- Watermark settings.
- ZIP export.
- Manifest CSV generation.
- Free single-use tool backend reuse.
- Usage tracking by plan.

Deliverable:

- User can export a processed ZIP with resized, compressed, watermarked, renamed images and manifest.

### Week 3: Payments, presets, and polish

Build:

- Stripe products and subscriptions.
- Billing portal.
- Plan gating.
- Saved presets.
- Recent pack history.
- Empty states.
- Error handling.
- Upload limit handling.
- Basic transactional emails via Resend.
- Responsive UI polish.

Deliverable:

- Paid gating works. Solo and Pro limits are enforced. User can save and reuse presets.

### Week 4: Final build work + marketing prep in parallel

Build:

- Public homepage.
- Free tools pages:
  - single image resizer
  - watermark cross-stitch pattern
  - bulk product photo renamer
- SEO guide page templates.
- Analytics events.
- Onboarding flow.
- Sample demo pack.
- QA with realistic batches.
- Production deployment to Coolify.

Deliverable:

- Production-ready beta with public SEO pages and functional paid workflow.

### Week 5: Launch

Build/support:

- Fix launch bugs.
- Improve onboarding based on first users.
- Add missing presets requested by beta users.
- Monitor processing errors and Stripe webhooks.
- Add one high-value SEO/tool page from launch feedback.

Deliverable:

- Stable public launch, first paid users, and clear product analytics.

## Score breakdown

- **recurringRevenue: 7/10** — Sellers who publish new designs monthly can justify recurring payment, but low-frequency hobby sellers may churn.
- **buildComplexity: 7/10** — Batch image upload, processing, watermarking, ZIP export, auth, and Stripe are feasible but require careful handling of files and limits.
- **marketValidation: 8/10** — Canva, Placeit, Vela, eRank, and Marmalead validate paid visual/seller workflows; Etsy’s large seller base supports a niche wedge.
- **competition: 7/10** — Generic image and design tools are strong, but they do not focus on marketplace-ready packaging, SKU naming, and digital-pattern theft deterrence.
- **automationPotential: 8/10** — The workflow is highly automatable: resize, compress, watermark, rename, order, and export.
- **platformFit: 9/10** — A browser-based Next.js SaaS with server-side image processing and ZIP export fits the Forge/Coolify pipeline well.
- **distributionFeasibility: 8/10** — Strong because the research identifies concrete long-tail SEO keywords and reachable seller communities; the main caveat is that actual keyword volumes were not provided and must be validated after launch.

## Build instructions for the coding agent

1. Initialize the project:

   `npx create-next-app@latest ecommerce-listing-image-packager-etsy-amazon-sellers --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`

2. Install required dependencies:

   `npm install prisma @prisma/client next-auth @auth/prisma-adapter stripe resend sharp archiver zod react-hook-form @hookform/resolvers lucide-react nanoid`

   Also install shadcn:

   `npx shadcn@latest init`

   Add components:

   `npx shadcn@latest add button card input textarea select tabs dialog dropdown-menu table badge slider switch form toast progress separator`

3. Configure Next.js for standalone deployment.

   In `next.config.ts`, set:

   - `output: "standalone"`
   - image remote patterns only if needed later.
   - server actions/body limits as needed for uploads, but prefer route handlers with multipart parsing.

4. Set up Prisma.

   Run:

   `npx prisma init`

   Define `prisma/schema.prisma` with these models:

   - `User`
     - `id`
     - `email`
     - `name`
     - `image`
     - `stripeCustomerId`
     - `plan`
     - `createdAt`
     - `updatedAt`

   - `Account`, `Session`, `VerificationToken`
     - standard NextAuth Prisma adapter models.

   - `Subscription`
     - `id`
     - `userId`
     - `stripeSubscriptionId`
     - `stripePriceId`
     - `status`
     - `currentPeriodEnd`
     - `createdAt`
     - `updatedAt`

   - `Preset`
     - `id`
     - `userId`
     - `name`
     - `marketplaceProfile`
     - `filenamePattern`
     - `resizeMode`
     - `maxWidth`
     - `maxHeight`
     - `quality`
     - `watermarkJson`
     - `slotConfigJson`
     - `createdAt`
     - `updatedAt`

   - `ImagePack`
     - `id`
     - `userId`
     - `name`
     - `sku`
     - `marketplaceProfile`
     - `settingsJson`
     - `manifestJson`
     - `status`
     - `createdAt`
     - `updatedAt`

   - `UsageEvent`
     - `id`
     - `userId`
     - `eventType`
     - `imageCount`
     - `packId`
     - `createdAt`

5. Generate Prisma client:

   `npx prisma generate`

   Add migration command for local development:

   `npx prisma migrate dev --name init`

6. Configure environment variables in `.env.example`:

   - `DATABASE_URL`
   - `NEXTAUTH_URL`
   - `AUTH_SECRET`
   - `GOOGLE_CLIENT_ID`
   - `GOOGLE_CLIENT_SECRET`
   - `STRIPE_SECRET_KEY`
   - `STRIPE_WEBHOOK_SECRET`
   - `STRIPE_SOLO_PRICE_ID`
   - `STRIPE_PRO_PRICE_ID`
   - `RESEND_API_KEY`
   - `EMAIL_FROM`
   - `NEXT_PUBLIC_APP_URL`

7. Set up NextAuth v5.

   Create:

   - `src/auth.ts`
   - `src/app/api/auth/[...nextauth]/route.ts`
   - `src/lib/auth.ts`

   Use Google OAuth and Prisma adapter. Add helper `getCurrentUser()` that returns the authenticated user or null.

8. Create app layout and navigation.

   Files:

   - `src/app/layout.tsx`
   - `src/app/page.tsx`
   - `src/app/(marketing)/layout.tsx`
   - `src/app/(app)/app/layout.tsx`
   - `src/components/site-header.tsx`
   - `src/components/app-sidebar.tsx`
   - `src/components/pricing-section.tsx`

   Marketing nav should link to Tools, Guides, Pricing, Sign in. App nav should link to Dashboard, New Pack, Presets, Billing.

9. Build pricing and Stripe setup.

   Files:

   - `src/lib/stripe.ts`
   - `src/app/api/stripe/checkout/route.ts`
   - `src/app/api/stripe/portal/route.ts`
   - `src/app/api/webhooks/stripe/route.ts`

   Implement:

   - Checkout session creation for Solo and Pro.
   - Billing portal session.
   - Webhook handling for subscription created, updated, deleted.
   - Update `User.plan` and `Subscription.status`.

10. Define plan limits.

   Create `src/lib/plans.ts`:

   Data shape:

   ```ts
   type PlanKey = "FREE" | "SOLO" | "PRO";

   type PlanLimits = {
     monthlyImages: number;
     maxImagesPerPack: number;
     savedPresets: number;
     amazonProfiles: boolean;
     manifestCsv: boolean;
   };
   ```

   Limits:

   - FREE: 10 monthly images, 5 per pack, 0 presets, Etsy only.
   - SOLO: 100 monthly images, 25 per pack, 3 presets, Etsy only.
   - PRO: 500 monthly images, 100 per pack, unlimited presets, Etsy + Amazon.

11. Build marketplace profile configuration.

   Create `src/lib/marketplace-profiles.ts`.

   Data shape:

   ```ts
   type SlotDefinition = {
     id: string;
     label: string;
     description: string;
     required?: boolean;
   };

   type MarketplaceProfile = {
     id: "ETSY_DIGITAL" | "ETSY_PHYSICAL" | "AMAZON";
     label: string;
     defaultMaxWidth: number;
     defaultMaxHeight: number;
     defaultQuality: number;
     defaultFilenamePattern: string;
     slots: SlotDefinition[];
   };
   ```

   Include Etsy digital, Etsy physical, and Amazon profiles.

12. Build the dashboard.

   Files:

   - `src/app/(app)/app/page.tsx`
   - `src/components/dashboard/recent-packs.tsx`
   - `src/components/dashboard/usage-card.tsx`
   - `src/components/dashboard/quick-actions.tsx`

   Show:

   - Current plan.
   - Images processed this month.
   - Recent packs.
   - CTA to create new pack.
   - CTA to upgrade if near limits.

13. Build new pack flow.

   Files:

   - `src/app/(app)/app/packs/new/page.tsx`
   - `src/components/pack/pack-wizard.tsx`
   - `src/components/pack/profile-step.tsx`
   - `src/components/pack/upload-step.tsx`
   - `src/components/pack/slot-order-step.tsx`
   - `src/components/pack/settings-step.tsx`
   - `src/components/pack/export-step.tsx`

   Wizard steps:

   1. Choose marketplace profile.
   2. Enter pack name and SKU/ASIN.
   3. Upload images.
   4. Assign/order slots.
   5. Configure resize/compression/watermark/filename pattern.
   6. Preview output.
   7. Export ZIP.

14. Implement upload handling.

   For MVP, process files during export rather than permanently storing originals.

   Create:

   - `src/components/pack/image-dropzone.tsx`
   - `src/lib/client-image-utils.ts`

   Client-side state shape:

   ```ts
   type UploadedImage = {
     id: string;
     file: File;
     originalName: string;
     previewUrl: string;
     width?: number;
     height?: number;
     size: number;
     assignedSlotId?: string;
     order: number;
   };
   ```

   Use browser APIs to read dimensions and display previews.

15. Implement image processing API.

   Create:

   - `src/app/api/packs/export/route.ts`
   - `src/lib/image-processing.ts`
   - `src/lib/zip.ts`
   - `src/lib/filename-patterns.ts`

   Request should accept multipart form data:

   - files
   - `profileId`
   - `sku`
   - `settingsJson`
   - `slotAssignmentsJson`

   Processing steps:

   - Validate auth.
   - Validate plan limits.
   - Parse settings with Zod.
   - For each image:
     - Resize with `sharp`.
     - Apply watermark if enabled.
     - Convert to JPG or WebP based on settings.
     - Generate output filename.
   - Generate `manifest.csv`.
   - Create ZIP with `archiver`.
   - Record `ImagePack` metadata.
   - Record `UsageEvent`.
   - Return ZIP download response.

16. Implement watermarking.

   In `src/lib/image-processing.ts`, support:

   - Text watermark.
   - Opacity.
   - Font size.
   - Rotation.
   - Diagonal repeated pattern.
   - Center.
   - Bottom-right.

   Use SVG overlay composed into `sharp`.

   Watermark settings shape:

   ```ts
   type WatermarkSettings = {
     enabled: boolean;
     text: string;
     mode: "DIAGONAL_REPEAT" | "CENTER" | "BOTTOM_RIGHT";
     opacity: number;
     fontSize: number;
     rotation: number;
     color: string;
   };
   ```

17. Implement filename pattern preview.

   Create `src/lib/filename-patterns.ts`.

   Support tokens:

   - `{sku}`
   - `{asin}`
   - `{slotNumber}`
   - `{slotName}`
   - `{original}`
   - `{profile}`

   Add `sanitizeFilename()` to remove unsafe characters. Show preview in `filename-preview-table.tsx`.

18. Build saved presets.

   Files:

   - `src/app/(app)/app/presets/page.tsx`
   - `src/app/api/presets/route.ts`
   - `src/app/api/presets/[id]/route.ts`
   - `src/components/presets/preset-form.tsx`
   - `src/components/presets/preset-list.tsx`

   Enforce plan preset limits. Allow users to apply a preset in the pack wizard.

19. Build free SEO tools.

   Use marketing routes:

   - `src/app/(marketing)/tools/page.tsx`
   - `src/app/(marketing)/tools/etsy-digital-pattern-image-size/page.tsx`
   - `src/app/(marketing)/tools/watermark-cross-stitch-pattern/page.tsx`
   - `src/app/(marketing)/tools/bulk-product-photo-renamer/page.tsx`
   - `src/app/(marketing)/tools/sku-image-renamer/page.tsx`
   - `src/app/(marketing)/tools/listing-image-packager/page.tsx`

   Components:

   - `src/components/tools/free-resizer.tsx`
   - `src/components/tools/free-watermarker.tsx`
   - `src/components/tools/free-renamer.tsx`

   Free tools can run client-side where practical. Add CTA cards after each export: “Create a full listing image pack.”

20. Build guide pages.

   Files:

   - `src/app/(marketing)/guides/sell-cross-stitch-patterns-on-etsy/page.tsx`
   - `src/app/(marketing)/guides/amazon-image-requirements/page.tsx`
   - `src/app/(marketing)/tools/etsy-cross-stitch-listing-photos/page.tsx`
   - `src/app/(marketing)/tools/cross-stitch-pattern-mockup-generator/page.tsx`
   - `src/app/(marketing)/tools/amazon-listing-image-organizer/page.tsx`

   Each page must include:

   - H1 matching the keyword.
   - Practical checklist.
   - Embedded free tool or demo.
   - CTA to create a full pack.
   - Internal links to related tools.

21. Add analytics events.

   Create `src/lib/analytics.ts`.

   Track:

   - `tool_used`
   - `signup_started`
   - `pack_created`
   - `images_uploaded`
   - `export_started`
   - `export_completed`
   - `checkout_started`
   - `subscription_created`

   Use a simple no-op wrapper unless PostHog or Plausible is configured.

22. Add email notifications with Resend.

   Files:

   - `src/lib/email.ts`
   - `src/emails/welcome-email.tsx`
   - `src/emails/export-complete-email.tsx`

   Send:

   - Welcome email after first sign-in.
   - Optional export-complete email for large exports if processing becomes async later.

   For MVP synchronous exports, welcome email is enough.

23. Add billing and upgrade UI.

   Files:

   - `src/app/(app)/app/billing/page.tsx`
   - `src/components/billing/plan-card.tsx`
   - `src/components/billing/usage-limits.tsx`

   Include:

   - Current plan.
   - Usage this month.
   - Upgrade/downgrade actions.
   - Manage billing button.

24. Add validation, errors, and limits.

   Implement:

   - File type validation.
   - File count validation.
   - File size validation.
   - Monthly usage validation.
   - Clear errors for unsupported files.
   - Graceful handling for failed ZIP generation.
   - Server-side Zod schemas for all API inputs.

25. Add production Docker support.

   Create `Dockerfile` for Next.js standalone deployment.

   Ensure:

   - `npm run build` passes.
   - Prisma generate runs during build.
   - App starts with `node server.js` from standalone output.
   - Environment variables are documented.

26. Final verification before deployment.

   Run:

   - `npm run lint`
   - `npm run build`
   - `npx prisma migrate deploy` against production DB
   - Stripe webhook test with Stripe CLI
   - Manual test:
     - Sign in with Google.
     - Create pack.
     - Upload 5 images.
     - Apply watermark.
     - Rename by SKU.
     - Export ZIP.
     - Verify filenames, image dimensions, compression, and manifest.
     - Upgrade to Solo in Stripe test mode.
     - Confirm plan updates after webhook.
   - Deploy to Coolify using Docker.


---

## PRD Completion Contract

This build is not complete until the implementation covers the whole PRD, not just a scaffold or a single happy path.

Before coding:

1. Read the PRD end-to-end.
2. Create `FORGE_PRD_TASKS.md`.
3. Break the PRD into an explicit checklist covering data model, auth, every user-facing page, every API/server action, every core workflow, billing/email/storage integrations or safe fallbacks, marketing/SEO pages, Docker/deploy config, and verification.

During coding:

1. Implement the checklist in dependency order: foundation -> data/auth -> core workflows -> secondary workflows -> marketing/pages -> deployment -> QA.
2. After each major phase, re-read the relevant PRD sections and update `FORGE_PRD_TASKS.md`.
3. Use realistic local/mock/safe-fallback implementations when external credentials are unavailable.
4. Do not leave placeholder pages, TODO-only routes, fake buttons, or unimplemented workflows.

Before finishing:

1. Run `npm run build` and fix all failures.
2. Start the dev server and smoke-test primary routes.
3. Create `FORGE_COMPLETION_AUDIT.md` mapping every major PRD requirement to concrete files/routes/components/actions that implement it.
4. List any truly external credential requirements in `HUMAN_INPUT_NEEDED.md`, but only after implementing guarded code paths and safe fallbacks.
5. Output `FORGE_BUILD_COMPLETE` only after the task ledger and audit exist.

---

## Dockerfile

**Forge writes the Dockerfile automatically after your build completes — do NOT author or modify a Dockerfile yourself.** It will be overwritten with the canonical template (`DOCKERFILE_SIMPLE` for apps without Prisma, `DOCKERFILE_PRISMA` if `package.json` includes `@prisma/client`).

Your responsibility: ensure `next.config.ts` (or `next.config.mjs`) sets `output: "standalone"` so the standalone bundle is produced by `npm run build`. If you skip that, the Dockerfile's `COPY .next/standalone` step fails at deploy time.

---

## File Organization

- Keep source code in `src/` (if applicable)
- Components in `src/components/`
- Utilities in `src/lib/` or `src/utils/`
- API routes in `src/app/api/` (Next.js) or equivalent
- Static assets in `public/`
- Environment variables in `.env.local` (never commit secrets)

---

## Build Verification

Run `npm run build` and ensure zero errors before committing.

- No TypeScript errors
- No ESLint warnings (fix or disable with justification)
- All pages/screens render without crashes
- API routes return expected responses

---

## Git Instructions

When the build is complete and verified:

1. Initialize the repository if not already done: `git init`
2. Stage all files: `git add -A`
3. Commit: `git commit -m "Initial build: ecommerce-listing-image-packager-etsy-amazon-sellers"`
4. Push to remote when instructed

---

## Required Files Checklist

- [ ] `README.md` - Project description, setup instructions, tech stack
- [ ] `.gitignore` - Appropriate for Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- [ ] `.env.example` - Template for required environment variables (see below)
- [ ] `package.json` (or equivalent) - Dependencies and scripts

---

## Zero-Config Deployment Requirement

**CRITICAL: This app MUST start and run correctly with NO environment variables configured in the deployment platform (Coolify). All external service dependencies must either be absent or have safe fallback defaults baked in.**

Apps that crash on startup due to missing env vars will fail every deployment cycle.

**CRITICAL: Never instantiate third-party API clients at module level.** Next.js evaluates top-level code during `next build` — a `new Resend(...)`, `new Stripe(...)`, or `new OpenAI(...)` call at module scope will crash the build if the API key env var is missing. Always initialize these clients **inside** the handler/function body, guarded by an env var check.

---

## Environment Variables (.env.example)

Only include variables the app actually uses. If the PRD doesn't require a feature, don't add those variables.

```bash
# App URL (optional — remove if not needed)
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# --- Only include below if PRD requires data persistence ---
# Database (SQLite — no external service required)
DATABASE_URL="file:./dev.db"

# --- Only include below if PRD requires user login ---
# NextAuth secret — a default is baked into the Dockerfile; override here for production
AUTH_SECRET="your-secret-here"  # generate with: openssl rand -base64 32

# --- Only include below if PRD explicitly requires payments ---
# STRIPE_SECRET_KEY="sk_live_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Only include below if PRD explicitly requires email ---
# RESEND_API_KEY="re_..."
# EMAIL_FROM="noreply@yourdomain.com"
```

---

## Key Implementation Notes

### Database (only if PRD requires persistence)
- Use **SQLite** via Prisma: `provider = "sqlite"`, `url = env("DATABASE_URL")`
- `DATABASE_URL` defaults to `file:/data/app.db` in the Dockerfile — no Coolify config needed
- For local dev: `DATABASE_URL="file:./dev.db"` in `.env.local`
- Run `npx prisma db push` after schema changes
- **Do NOT use PostgreSQL** — no PostgreSQL server is provisioned in the deployment environment

### Authentication (only if PRD requires user login)
- Use **NextAuth v5** with the **Credentials provider** (email + password)
- **Do NOT use Google OAuth** — no OAuth credentials are configured in deployment
- Hash passwords with **bcrypt** (`bcryptjs` package)
- Session strategy: **`"jwt"`** (not `"database"`) — required for Credentials provider

### Public Routes Contract (CRITICAL when adding auth)

If you add auth, the following routes MUST render to status 200 for an unauthenticated visitor — they are how customers find the product, evaluate it, and sign up:

- `/` — homepage / landing
- `/pricing` — pricing page (if pricing exists)
- `/blog`, `/blog/[slug]` — blog (if any)
- `/about`, `/contact`, `/legal/*` — marketing supporting pages
- `/login`, `/signup` — auth entry points themselves

Implementation rules:
- Place public pages OUTSIDE any `(app)` / `(authenticated)` / `(dashboard)` route group, AND outside any layout that calls `requireUser()` / `auth()` / `getServerSession()`.
- Authenticated routes go INSIDE a route group whose layout gates with `requireUser()` (e.g. `src/app/(app)/dashboard`, `src/app/(app)/settings`, `src/app/(app)/billing` if billing is the user's own subscription page). The verifier knows about Next.js route groups and expects these to redirect to `/login` when visited anonymously.
- If you write a `middleware.ts`, its `matcher` MUST exclude the public routes above. A blanket matcher like `/((?!api|_next).*)` that protects everything is a build failure.
- A user with no session who visits `/` and lands on `/login` is a build failure — Forge's verifier will detect it and reject the build.

If the PRD does NOT require auth, do NOT add NextAuth at all. No middleware, no `requireUser`, no `(app)` group — just public pages.
- `AUTH_SECRET` defaults are baked into the Dockerfile; app works out of the box

### Payments (only if PRD explicitly requires it)
- Use Stripe only when the PRD's core functionality requires payment processing
- If adding Stripe: handle `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted` at `/api/webhooks/stripe`
- Make the app functional without Stripe credentials (show upgrade prompts rather than crashing)

### Email (only if PRD explicitly requires it)
- Use Resend only when the PRD requires transactional email
- **CRITICAL: NEVER instantiate `new Resend(...)` at module level** — always inside the handler function body with a guard:
  ```ts
  // WRONG — causes build failure (next build evaluates module-level code):
  const resend = new Resend(process.env.RESEND_API_KEY);
  export async function POST(req) { ... }

  // CORRECT — lazy init inside handler:
  export async function POST(req) {
    if (!process.env.RESEND_API_KEY) {
      console.log("[email] RESEND_API_KEY not set, skipping email");
      return NextResponse.json({ ok: true });
    }
    const { Resend } = await import("resend");
    const resend = new Resend(process.env.RESEND_API_KEY);
    ...
  }
  ```
- Next.js evaluates module-level code during `next build` page data collection — missing env vars at module scope crash the build
- Same rule applies to Stripe, OpenAI, and ALL third-party SDK clients — always lazy-initialize inside handler functions

### Fonts
- **Do NOT use `next/font/google`** — it fetches font files during `next build`, which can hang or fail in automated/offline build environments.
- Use CSS/system font stacks by default.
- If a custom font is truly required, commit the font file into the repo and use `next/font/local`; never depend on a build-time network font fetch.

### Deployment
- Coolify with Docker — always set `output: "standalone"` in next.config.ts
- Do NOT add `COPY public ./public` in the Dockerfile if the public/ directory is empty or doesn't exist
- The Dockerfile's CMD initializes the SQLite database on first start via `prisma db push`
- App must respond healthy on port 3000 within 60 seconds of container start
- **CRITICAL Dockerfile rule**: In the `deps` stage, always use `RUN npm ci --ignore-scripts`. Without `--ignore-scripts`, the `postinstall` hook (e.g. `prisma generate`) runs before `prisma/schema.prisma` is copied, causing the build to fail. Run `npx prisma generate` explicitly in the `builder` stage after `COPY . .`.
- **Do NOT run `npx prisma db push` in the Dockerfile builder stage** — it is not needed at build time. The CMD already runs `prisma db push` at container startup when a real `/data/app.db` path is available.
- **Use `node:20-slim` (not `node:20-alpine`)** — Debian slim avoids Alpine musl/libssl issues. Add `RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*` to BOTH the builder and runner stages for Prisma's schema engine.
- **Set `binaryTargets` in schema.prisma** — always include `binaryTargets = ["native", "debian-openssl-3.0.x"]` in the Prisma generator block so Prisma generates the correct engine binary for the Debian container environment.
- **Copy full node_modules in the runner stage** — do NOT selectively copy `node_modules/.prisma`, `node_modules/prisma`, `node_modules/@prisma` separately. Instead use `COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules` to copy everything. Prisma v6+ CLI has deep transitive deps (`@prisma/config` → `effect`, `c12`, etc.) that break if not all present.

